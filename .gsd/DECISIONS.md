# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | 2025-03-22 | init | GSD structure initialized | Created PROJECT.md, STATE.md, REQUIREMENTS.md, DECISIONS.md, KNOWLEDGE.md | Project needs structured milestone tracking | Yes |
| D002 | 初始化 GSD 与现有文档系统集成 | architecture | GSD 与现有 docs 系统并存策略 | 采用 GSD SQLite 状态机 + 现有 docs/ 文档系统并存的混合架构 | 现有 docs/ 系统包含 197 个 Markdown 文件，涵盖治理规则、模块文档、变更记录等丰富内容。GSD 提供结构化状态管理能力。两者互补：GSD 管理需求/决策状态，docs/ 保留详细文档。通过 .gitignore 只保存 GSD 生成的 Markdown，不保存 gsd.db。 | Yes |
| D003 | GSD 初始化后补全项目信息 | architecture | GSD 项目初始化补全 | 通过读取现有 docs/ 系统逆向工程，补全 GSD 项目信息：识别三个模块（app4, quantaalpha, backtest），77个变更文档，3个ADR | GSD 自动检测只识别了 app4 模块。通过读取 docs/02-modules/*.md 和扫描 docs/03-changes/，发现项目实际包含三个核心模块：app4（数据管道）、quantaalpha（因子挖掘）、backtest（策略回测）。已有 77 个变更文档（4个完成，12个进行中/计划中），3个架构决策记录。 | Yes |
| D004 | 评估 ADR-003 完成度，决定模块边界设计 | architecture | ADR-003 外插模块边界与职责分离 | ADR-003 保持 draft 状态，待评估后决定接受、修改或拒绝 | ADR-003 定义了 quantaalpha 主链与外插模块（orchestrator、trigger、observability、test harness、registry）的边界。当前状态为 draft，需要评估：1) 模块边界是否清晰；2) 集成契约是否可行；3) 分阶段落地计划是否合理。此决策影响 24 小时连续运行系统的架构。 | Yes |
| D005 | 评估 ADR-001 完成度，决定架构方向 | architecture | ADR-001 持续因子研究系统演进架构方向 | ADR-001 保持 draft 状态，待评估后决定接受、修改或拒绝 | ADR-001 定义了多模型研究、多周期验证、因子知识库、数据能力注册表、持续运转调度五层架构。当前状态为 draft，需要评估：1) 哪些组件已部分实现；2) 哪些需要调整；3) 是否接受此架构方向。此决策影响 quantaalpha 模块的长期演进。 | Yes |
| D006 | M001 | process | QuantaAlpha Bug 修复里程碑存储位置 | 使用 GSD 标准里程碑目录结构存储修复计划 | 根据 GSD 规范，修复计划作为里程碑 M001 管理，文档存放在 .gsd/milestones/M001/ 下。包含 M001-CONTEXT.md（问题描述）、M001-ROADMAP.md（路线图）、slices/S01/S01-PLAN.md（切片计划）。 | Yes |
| D007 | M001 文档修正 | process | M001 文档修正内容 | 更新 M001 文档以反映工程师审查意见 | 根据4位工程师的审查报告，对 M001 文档进行了以下关键修正：1) 明确说明"4个高优先级Bug"而非"全部问题"，补充第5个Bug（'dict' object has no attribute 'replace'）将在M002处理；2) Bug 3 阶段改为 factor_construct（不是 proposal）；3) Bug 1 收缩范围为 client.py:69-74（主要触发点），client.py:667 和 universe.py:111 标注为"同类隐患"；4) 空响应检查移至 S02，与无限重试修复合并；5) 修正 S03 预期日志消息为 "Fixed JSON format issues"（与实际代码一致）；6) 修正 grep 验证命令，避免多行匹配问题。 | Yes |
| D008 | M001 完成 | process | M001 文档状态更新 | 更新所有 M001 文档，标记 S01/S02/S03 为已完成 | 所有三个切片的修复代码已完成并验证通过。更新了 M001-ROADMAP.md、S01-PLAN.md、S02-PLAN.md、S03-PLAN.md，添加了修复完成记录和验证结果。剩余工作：运行因子挖掘验证无卡死现象，将修复记录到 KNOWLEDGE.md。 | Yes |
| D009 | M001 完成 | process | 创建 KNOWLEDGE.md | 创建 KNOWLEDGE.md 文件，记录 M001 修复经验和项目知识 | KNOWLEDGE.md 是 GSD 规范要求的知识寄存器，用于记录项目特定的规则、模式和经验教训。M001 修复完成后，将关键教训、验证方法和未解决问题记录到 KNOWLEDGE.md，便于后续开发者和 AI Agent 参考。 | Yes |
| D010 | 2026-03-22 综合规划评审通过 | architecture | QuantaAlpha 持续因子挖掘系统演进方案与技术基线确立 | 批准并全面采用《QuantaAlpha 持续因子挖掘体系综合落地规划》及其 7 项技术增补（S1-S7） | 代码级差距分析揭示了现有系统在数据感知（Data Capability）、模型鲁棒性（ProviderPool）、异常恢复（Checkpoint）和 24H 自治（Orchestrator）方面的关键缺失。综合规划及 S1-S7 增补项针对性地解决了这些断裂点，为系统从"单点工具"向"持续研究体"演进提供了清晰的技术路线。此决策将 ADR-001 和 ADR-003 的架构方向正式转化为可执行的工程任务。 | Yes |
| D011 | 2026-03-22 实施规划制定完成 | process | QuantaAlpha 持续因子挖掘体系实施路线图确立 | 采用三阶段滚动实施路线图（Phase 1: 防御与觉察；Phase 2: 分层计算与多模型；Phase 3: 无人值守网络） | 通过分阶段实施，系统可以在短期内（Phase 1）通过配置修正（排除北交所、多周期回测）获得显著收益；中期（Phase 2）建立模型路由和数据感知等核心鲁棒性组件；长期（Phase 3）完成自治调度和资源管理，确保 24 小时无人值守。这种迭代方式有效平衡了系统演进的复杂度和交付价值的持续性。 | Yes |
| D012 | 2026-03-22 综合规划评审通过 | architecture | Parquet 数据集成与双轨计算架构设计方案确立 | Parquet 数据生态的物理挂载采用 Registry 配置代理 + 底层双轨计算架构（Pipeline Dump + Polars Engine） | 针对 /data/*.parquet 下海量资产，Registry 负责动态扫描 schema 并注入时滞约束至 LLM。双轨制确保了兼容性（Pipeline Dump 至 Qlib Bin 适配现有策略）与高性能（Polars Engine 直读直算大规模因子）。此架构能有效隔离数据存储、因子解析与回测模拟的关注点，适配 ADR-003 的外插模块化边界。 | Yes |
| D013 | 2026-03-22 综合规划评审通过 (S6 补充项) | architecture | 因子计算层 Point-in-Time (PIT) 强约束执行方案确立 | 在因子回测计算层强制执行 Point-in-Time (PIT) 对齐（按 ann_date 动态对齐 + 滞后 lag_days 约束） | 针对季度财报等非日频数据，LLM 易生成带有"未来函数"的表达式。S6 方案通过在计算层引入 PIT 动态对齐机制（只有当公告日 ann_date <= T 时才可见数据），能从根本上消除因子回测中的偏误。此方案作为 P2 核心技术支柱，直接关联系统产出因子的真实有效性。 | Yes |
| D014 | 2026-03-22 综合规划评审通过 | architecture | 正式批准并采用 ADR-001 持续因子研究系统演进架构 | 批准 ADR-001 为正式架构文档，进入实施阶段；Supersedes D005 | ADR-001 定义了多模型研究、多周期验证、因子知识库、数据能力注册表、持续运转调度五层架构。经过综合规划评审，确认该架构方向正确且与现有代码基线兼容。Phase 1 实施将基于 ADR-001 展开，包括数据能力注入、ProviderPool 多模型路由、因子知识库增强等核心组件。D005 的 draft 状态评估完成，正式批准该架构方向。 | Yes |
| D015 | 2026-03-22 综合规划评审通过 | architecture | 正式批准并采用 ADR-003 外插模块边界与职责分离架构 | 批准 ADR-003 为正式架构文档，进入 Phase 3 实施规划；Supersedes D004 | ADR-003 定义了 quantaalpha 主链与外插模块（orchestrator、trigger、observability、test harness、registry）的边界。经过综合规划评审，确认模块边界清晰、集成契约可行、分阶段落地计划合理。该架构为 24 小时连续运行系统提供了清晰的扩展路径。D004 的 draft 状态评估完成，正式批准该架构方向。 | Yes |
| D016 | 2026-03-22 综合规划评审通过 (S2 补充项) | architecture | ProviderPool 多模型管理架构设计方案确立 | 摒弃全局单例 APIBackend，引入包含 Fanout 并发、轮询降级及角色分配的动态 ProviderPool 底层路由架构 | 当前 `client.py` 单线条执行，缺乏多 Provider 实例支持、负载分担、健康状态追踪和自动降级能力。S2 方案通过 ProviderPool 封装层实现：1) 多 Provider 并存（不同模型/厂商）；2) 按任务类型路由（hypothesis/coding/screening）；3) Fanout 并发取最优；4) 健康状态监控与自动降级；5) Token 使用追踪。此架构直接解决 M001 Bug 2（空响应无限重试）的教训，强制引入重试上限和 Provider 切换机制。 | Yes |
| D017 | 2026-03-22 综合规划评审通过 (S5 补充项) | architecture | 异常恢复、幂等性与 Checkpoint 机制设计方案确立 | 引入中断保存机制（进程崩溃可恢复状态机）以及因子在数据库中的多版本历史留存机制 | 24 小时持续运行场景下，进程崩溃会导致整轮进度丢失。S5 方案通过：1) LoopCheckpoint 在每个 pipeline 步骤后持久化状态；2) 因子库增加 versions 字段保留历史 backtest 结果；3) 文件锁增加超时机制防止死锁。此架构确保系统具备故障恢复能力，支持"断点续挖"而非重新开始。 | Yes |
| D018 | 2026-03-22 综合规划评审通过 (S7 补充项) | architecture | 24H 自治运行资源管理约束设计方案确立 | 引入 ResourceManager 模块，对每日 Token 消耗预算、磁盘空间、result.h5 生命周期做强制拦截和垃圾回收 | 持续运行会导致 API 成本、磁盘空间、内存使用不可控。S7 方案通过 ResourceManager 实现：1) 每日 Token 预算硬上限（默认 5M tokens）；2) 磁盘空间监控与告警（<5GB 触发）；3) result.h5 自动清理（默认保留 30 天）；4) 因子库条目上限与 SQLite 迁移阈值。这些约束确保系统在资源边界内安全运行，防止成本失控和磁盘溢出。 | Yes |
| D019 | 2026-03-22 综合规划评审通过 | process | M001 历史故障经验转化为新系统设计约束 | 将 M001 Bug 修复中的关键教训（JSON 死循环、空响应无限重试、dict.replace 类型错误）作为 S2/S3 设计的强制性约束条件 | M001 修复经验揭示了系统鲁棒性的关键缺口。根据 KNOWLEDGE.md 记录，强制要求：1) ProviderPool 必须区分空响应和网络错误，空响应立即切换 Provider；2) Coding 模型 JSON 修复必须设置超时和重试上限；3) 数据能力注册表处理 schema 字段时需类型安全检查；4) Checkpoint 序列化需验证含换行符字段的兼容性。这些约束直接写入 S2/S3/S5 的设计文档和验收标准。 | Yes |
| D020 | 2026-03-22 Bug #5 修复启动 | process | M002 里程碑立项：QuantaAlpha 数据类型 Bug 修复 | 使用 GSD 标准里程碑目录结构管理 Bug #5 修复（'dict' object has no attribute 'replace'） | 根据 D007 备注，M001 推迟处理的第 5 个 Bug（consistency check 数据类型问题）独立立项为 M002。里程碑包含 M002-CONTEXT.md（问题描述）、M002-ROADMAP.md（路线图）、slices/S01/S01-PLAN.md（切片计划）。与 M001 保持一致的文档结构和流程。 | Yes |
| D021 | 2026-03-22 架构实施启动 | process | M003 里程碑立项：QuantaAlpha 持续因子挖掘体系架构实施 | 使用 GSD 标准里程碑目录结构管理 D014-D019 架构决策的实施 | 根据 D014-D019 批准的架构决策，需要可执行的工程任务承载实施工作。M003 里程碑包含：M003-CONTEXT.md（决策来源与目标）、M003-ROADMAP.md（10 个切片路线图）、slices/S01-S10/（具体切片计划）。涵盖 ProviderPool、Checkpoint、ResourceManager、PIT 对齐、ADR-003 设计等核心组件。 | Yes |
| D022 | 2026-03-23 S01 Bug 定位完成 | bug-fix | M002 S01 Bug 定位结论 | Bug 位于 consistency_checker.py:265，当 LLM 返回 dict 类型的 corrected_expression 时触发 | S01 精确定位了 `'dict' object has no attribute 'replace'` 错误的触发位置。根因是 LLM JSON 响应中 `corrected_expression` 字段可能包含嵌套 dict 结构（如 `{"code": "close/open", "note": "..."}`），而 `ComplexityChecker.check()` 第 265 行的 `expression.replace(" ", "")` 假设 expression 是字符串。修复方案：在 `FactorQualityGate.evaluate()` 第 487 行后调用 `normalize_corrected_expression()` 进行类型转换（该函数已存在于 proposal.py:23-26）。 | Yes |
| D023 | 2026-03-23 S02 Bug 修复完成 | bug-fix | M002 S02 防御性类型检查修复方案 | 在 ComplexityChecker.check() 和 RedundancyChecker.check() 方法入口添加 isinstance(expression, dict) 检查 | S02 实现了防御性修复，在两个 checker 方法入口添加 isinstance() 守卫，而非仅在 FactorQualityGate.evaluate() 单点调用 normalize_corrected_expression()。此方案更健壮：1) 任何直接调用 check() 的代码都能受益；2) 与 proposal.py 的 normalize_corrected_expression() 模式一致；3) 提供 graceful fallback 链（code → expression → str）。验证：13 项单元测试全部通过。 | Yes |
| D024 | 2026-03-23 S03 回归测试完成 | bug-fix | M002 S03 回归测试固化方案 | 创建独立回归测试文件 + 更新项目知识库 | S03 完成两项交付：1) 在 tests/ 和 third_party/quantaalpha/tests/ 创建 25 个回归测试用例（test_dict_replace_bug_fix.py 12个 + test_dict_replace_fix_unit.py 13个）；2) 在 KNOWLEDGE.md 追加 M002 S03 章节记录关键教训。采用独立测试文件策略规避 quantaalpha pydantic 环境问题，同时确保 pytest 发现兼容性。 | Yes |
| D025 | 2026-03-24 缺口分析完成 | process | M004 里程碑立项：因子库深化与自治能力增强 | 使用 GSD 标准里程碑目录结构管理 factor_mining_requirements.md 中未被 M003 覆盖的需求缺口（A-J） | 根据 docs/drafts/mining/需求与GSD里程碑对照表.md 识别的 10 个缺口，创建 M004 里程碑（8 个 Slice）覆盖：跨周期验证标准(H)、因子重验候选(F)、分类标签(C)、注册表扩展(G)、生命周期状态机(E)、RAG 向量检索(D)、Ensemble 聚合(A+B)、24H 调度中心(I+J)。 | Yes |
| D026 | 2026-03-24 S01 实现完成 | validation | S01 跨周期验证通过标准实现方案 | 使用 dataclass (EvaluationResult) 作为结构化返回类型，配置 + 函数双交付 | S01 实现了跨周期验证 pass_criteria 自动判定系统：1) backtest.yaml 新增 require_all_pass/min_ic/min_rank_ic/min_periods_pass 配置；2) validation_judge.py 实现 evaluate_multi_period_results() 判定函数；3) 采用 dataclass 而非 dict 提供类型安全；4) 防御性处理 None 值和空列表。IC 和 Rank IC 使用严格大于 (>) 比较，必须同时满足两个阈值才视为周期通过。已通过 5 种场景测试验证。 | Yes |
| D027 | 2026-03-24 S02 实现完成 | factor-library | S02 因子复验候选选择实现方案 | select_revalidation_candidates() 方法 + last_validated 初始化，使用 setdefault() 模式保护已存在值 | S02 实现了因子复验候选自动筛选系统：1) library.py 添加 select_revalidation_candidates(days, status, factor_ids) 筛选方法；2) _normalize_factor_entry() 使用 setdefault() 初始化 last_validated 为 datetime.now().isoformat()，确保既初始化新条目又保护已存在值；3) status_rules.py 的 update_factor_status() 已正确更新 last_validated。关键设计决策：last_validated=None 的因子始终被包含在结果中（跳过 days 过滤），因为未知验证历史的因子应参与复验考虑。已通过 15 项单元测试验证。 | Yes |
| D028 | 2026-03-24 S07 实现完成 | llm-routing | S07 Ensemble 聚合层实现方案 | 使用 dataclass + 策略函数模式，RDAgentLog 兼容性处理 | S07 实现了多模型 Ensemble 聚合和 ProviderPool 路由系统。关键决策：1) 策略函数（`_intersection_strategy` 等）不依赖 logger，避免 RDAgentLog.debug() 不存在的 AttributeError；2) LatencyStats 使用 in-place reset 而非 dict 删除，保持 get_latency_stats() 返回一致的字典结构；3) least_latency 样本不足时优雅降级到 round_robin；4) 使用 threading.RLock 保证并发安全。验证：54 项单元测试全部通过。 |
| D029 | 2026-03-24 缺口分析完成 | process | M005 里程碑立项：Mining Pipeline 关键 Bug 修复 | 使用 GSD 标准里程碑目录结构管理 6 个已验证 Bug（Bug-1 至 Bug-6）的修复工作 | 根据 `docs/drafts/mining/problems/20260324_bug_fix_plan.md` 和 `20260324_verified_bugs_and_fixes.md` 识别并验证的 6 个 Bug，按优先级立项为 M005（6 个 Slice）：S01 rdagent.log 硬依赖(P0)、S02 normalize_corrected_expression 强化(P0)、S03 consistency prompt 约束(P0)、S04 BadRequest 快速失败(P1)、S05 proposal.yaml 歧义清除(P2)、S06 JSON 修复集中化(P2)。修复顺序：S01 → S03+S02 → S04 → S05 → S06。 | Yes |
| D030 | 2026-03-24 S01 实现完成 | log-compat | rdagent.log 可选依赖 fallback 方案 | FallbackLoggerWrapper 包装 stdlib logging.Logger，object.__setattr__ 避免属性代理冲突 | M005 S01 的核心问题：rdagent 可导入但 rdagent.log 模块不存在。方案：try-except ImportError 守卫 + FallbackLoggerWrapper 包装 stdlib logging.Logger，添加 log_trace_path/set_trace_path/storage 接口。FallbackFileStorage 的 truncate() 为 no-op（fallback 模式不写文件）。LogColors 始终存在 fallback 版本，rdagent 提供时覆盖。两份 log/__init__.py（主目录 + vendored）必须保持 byte-identical，验证用 diff 或 MD5。关键实现细节：__setattr__ 中用 object.__setattr__(self, name, value) 写入私有属性，避免 __getattr__/__setattr__ 代理循环。 | Yes |
| D031 | 2026-03-24 S02 实现完成 | expression-parsing | normalize_corrected_expression 强化实现方案 | dict-first 处理 + embedded DSL regex + JSON string dict 解析 | M005 S02 实现了硬化版 normalize_corrected_expression 函数。关键设计决策：1) dict-first 处理必须在 isinstance(str) 守卫之前，避免 str(dict) 转换丢失 key 提取机会；2) embedded DSL regex `([A-Z][A-Z_]*\s*\([^)]+\))` 用于从 "Option A: STD(...)" 等模式提取 DSL；3) JSON string dict 解析处理输入是字符串形式的 dict 而非实际 dict 对象的情况；4) 两份 proposal.py 必须保持 byte-identical，cp to sync + diff to verify。验证：16 项单元测试全部通过。 | Yes |

| D032 | 2026-03-24 S03 实现完成 | prompt-constraint | consistency prompt 输出约束收紧方案 | 系统 prompt 字段约束 + 用户 prompt IMPORTANT 约束块 | M005 S03 收紧 consistency_prompts.yaml 的 corrected_expression 输出约束。关键决策：1) 系统 prompt 直接在 JSON 字段描述中嵌入约束措辞（"single-line DSL expression only — no markdown, no comments..."）；2) 用户 prompt 末尾添加 `**IMPORTANT:**` 粗体块，列举所有禁止模式；3) 两者互补——字段描述是规范，IMPORTANT 块是强调。采用 grep 验证约束措辞存在，无需运行时测试。验证：3 项 grep 检查全部通过。 | Yes |
| D033 | 2026-03-24 S05 实现完成 | prompt-config | proposal.yaml 死赋值移除与归档方案 | 删除死赋值 + 归档而非删除配置文件 | M005 S05 移除 proposal.py 中的配置歧义。关键决策：1) 删除第 159 行的死赋值（原指向 proposal.yaml），保留第 304 行有效赋值（指向 prompts.yaml）；2) proposal.yaml 归档为 .archived 而非删除，保留历史参考；3) 归档文件不应被 Python 导入系统加载。两项验证：删除后 `rg "qa_prompt_dict = Prompts" proposal.py` 仅返回 1 行，py_compile 通过。验证：4 项检查全部通过。 | Yes |
