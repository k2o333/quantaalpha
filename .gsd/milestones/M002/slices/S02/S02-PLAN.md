# S02: 实现类型检查与转换逻辑

**Goal:** 在定位出的出错点附近加上防御性代码或显式类型转换，修复数据类型错位引发的 Bug。
**Demo:** 之前因为 `dict` 对象无法调用 `.replace()` 而报错的方法，现在能正常处理该数据，整个程序得以继续推进。

## Must-Haves
- 出错行前加上对变量类型的分支判断。
- 逻辑能妥善兼容原有的 `str` 场景与新增的 `dict` 场景。

## Tasks

- [x] **T01: 实现类型适配防御代码**
  修改引发错误的核心方法。

- [x] **T02: 本地重跑最小复现脚本验证代码**
  运行 S01 编写的复现逻辑，确认修修复效果。

## Files Likely Touched
- 触发错误的 Python 源码文件（待S01报告指出）
