# 增量下载去重功能修复方案 v2

**目标**：确保四种类型的 stock_loop 接口都能正确实现"下载层面"的增量去重

---

## 一、问题根因

当前 [`UpdateManager.update_interface()`](app4/update/update_manager.py:213) 的缺口检测逻辑只对 `date_range` 或 `reverse_date_range` 模式生效：

```python
# update_manager.py L255-257
if (options.gap_detection_enabled and
    self.coverage_manager and
    self.coverage_manager.is_time_range_mode(interface_name)):  # ← stock_loop 返回 False
    # 缺口检测逻辑（被跳过）
```

而所有 stock_loop 模式的接口（类型 A/B/C/D）都会跳过这个检测，直接执行全量下载。

---

## 二、接口分类与修复策略

### 类型 A：交易日历接口

| 接口 | 查询参数 | 数据日期字段 | 缺口检测方式 |
|------|---------|-------------|-------------|
| `cyq_chips` | `start_date`, `end_date` | `trade_date` | 交易日历检测 |
| `moneyflow_dc` | `start_date`, `end_date` | `trade_date` | 交易日历检测 |
| `stk_factor_pro` | `start_date`, `end_date` | `trade_date` | 交易日历检测 |

**修复策略**：调用 [`CoverageManager._detect_trade_date_gaps()`](app4/core/coverage_manager.py:800)

---

### 类型 B：报告期接口

| 接口 | 查询参数 | 数据日期字段 | 缺口检测方式 |
|------|---------|-------------|-------------|
| `income_vip` | `start_date`, `end_date` | `end_date` | 报告期检测 |
| `balancesheet_vip` | `start_date`, `end_date` | `end_date` | 报告期检测 |
| `cashflow_vip` | `start_date`, `end_date` | `end_date` | 报告期检测 |
| `fina_indicator_vip` | `start_date`, `end_date` | `end_date` | 报告期检测 |
| `fina_audit` | `start_date`, `end_date` | `end_date` | 报告期检测 |
| `fina_mainbz_vip` | `start_date`, `end_date` | `end_date` | 报告期检测 |
| `forecast_vip` | `start_date`, `end_date` | `end_date` | 报告期检测 |
| `top10_floatholders` | `start_date`, `end_date` | `end_date` | 报告期检测 |

**修复策略**：调用 [`CoverageManager._detect_report_period_gaps()`](app4/core/coverage_manager.py:871)

---

### 类型 C：日期锚定接口

| 接口 | 查询参数 | 锚定参数 | 缺口检测方式 |
|------|---------|---------|-------------|
| `disclosure_date` | `end_date` (单个) | `end_date` | 锚点遍历 |
| `top10_holders` | `period` (单个) | `period` | 锚点遍历 |
| `dividend` | `ann_date` (单个) | `ann_date` | 锚点遍历 |
| `pledge_stat` | `end_date` (单个) | `end_date` | 锚点遍历 |
| `stk_rewards` | `end_date` (单个) | `end_date` | 锚点遍历 |

**修复策略**：调用 [`CoverageManager._detect_date_anchor_gaps()`](app4/core/coverage_manager.py:983)

---

### 类型 D：无日期过滤接口

| 接口 | 查询参数 | 特点 |
|------|---------|------|
| `pledge_detail` | `ts_code` (仅此一个) | 返回该股票所有质押明细 |

**修复策略**：调用 [`CoverageManager._detect_no_date_filter_gaps()`](app4/core/coverage_manager.py:1065)

---

## 三、代码修改方案

### 3.1 修改 `UpdateManager.update_interface()`

**文件**：[`app4/update/update_manager.py`](app4/update/update_manager.py)

**修改位置**：L254-308

```python
# 原代码（L254-308）
if (options.gap_detection_enabled and
    self.coverage_manager and
    self.coverage_manager.is_time_range_mode(interface_name)):
    # ... 缺口检测逻辑

# 修改为：
if options.gap_detection_enabled and self.coverage_manager:
    # 检查是否启用股票级别缺口检测
    detection_config = interface_config.get('duplicate_detection', {})
    stock_level_detection = detection_config.get('stock_level_detection', False)
    
    if stock_level_detection:
        # 使用股票级别缺口检测（适用于 stock_loop 模式）
        return self._update_with_stock_gap_detection(
            interface_name, interface_config, date_range, options
        )
    elif self.coverage_manager.is_time_range_mode(interface_name):
        # 使用接口级别缺口检测（适用于 date_range 模式）
        # ... 原有的缺口检测逻辑
```

### 3.2 新增 `_update_with_stock_gap_detection()` 方法

**文件**：[`app4/update/update_manager.py`](app4/update/update_manager.py)

**新增方法**：

```python
def _update_with_stock_gap_detection(
    self,
    interface_name: str,
    interface_config: Dict[str, Any],
    date_range: DateRange,
    options: UpdateOptions
) -> InterfaceUpdateResult:
    """
    使用股票级别缺口检测执行更新（适用于 stock_loop 模式）
    
    支持四种接口类型：
    - 类型 A：交易日历接口
    - 类型 B：报告期接口
    - 类型 C：日期锚定接口
    - 类型 D：无日期过滤接口
    """
    start_time = time.time()
    
    try:
        # 获取股票列表
        stock_list = self.downloader._get_stock_list()
        if not stock_list:
            logger.warning(f"无法获取股票列表，跳过 {interface_name}")
            return InterfaceUpdateResult(
                interface_name=interface_name,
                status=UpdateStatus.SKIPPED,
                date_range=date_range,
                skip_reason="无法获取股票列表",
                duration_seconds=time.time() - start_time
            )
        
        # 如果用户指定了 ts_code，只处理该股票
        if options.ts_code:
            stock_list = [s for s in stock_list if s.get('ts_code') == options.ts_code]
            if not stock_list:
                logger.warning(f"未找到股票 {options.ts_code}")
                return InterfaceUpdateResult(
                    interface_name=interface_name,
                    status=UpdateStatus.SKIPPED,
                    date_range=date_range,
                    skip_reason=f"未找到股票 {options.ts_code}",
                    duration_seconds=time.time() - start_time
                )
        
        total_records = 0
        user_provided_dates = options.start_date is not None and options.end_date is not None
        
        for stock in stock_list:
            ts_code = stock.get('ts_code')
            
            # 检测该股票的数据缺口
            gap_tasks = self.coverage_manager.detect_stock_gaps(
                interface_name=interface_name,
                ts_code=ts_code,
                start_date=date_range.start_date,
                end_date=date_range.end_date,
                interface_config=interface_config,
                user_provided_dates=user_provided_dates,
                stock_info=stock
            )
            
            if not gap_tasks:
                logger.debug(f"[{interface_name}/{ts_code}] 数据已完整，跳过")
                continue
            
            logger.info(f"[{interface_name}/{ts_code}] Gap detection found {len(gap_tasks)} tasks to download")
            
            # 逐个执行缺口下载任务
            for task_params in gap_tasks:
                records = self._execute_gap_task(
                    interface_name, interface_config, task_params, options
                )
                total_records += records
        
        duration = time.time() - start_time
        logger.info(f"接口 {interface_name} 更新完成，共 {total_records} 条记录，耗时 {duration:.2f}秒")
        
        return InterfaceUpdateResult(
            interface_name=interface_name,
            status=UpdateStatus.SUCCESS,
            date_range=date_range,
            record_count=total_records,
            duration_seconds=duration
        )
        
    except Exception as e:
        duration = time.time() - start_time
        logger.error(f"接口 {interface_name} 更新失败: {e}")
        return InterfaceUpdateResult(
            interface_name=interface_name,
            status=UpdateStatus.FAILED,
            error_message=str(e),
            duration_seconds=duration
        )
```

### 3.3 新增 `_execute_gap_task()` 方法

**文件**：[`app4/update/update_manager.py`](app4/update/update_manager.py)

**新增方法**：

```python
def _execute_gap_task(
    self,
    interface_name: str,
    interface_config: Dict[str, Any],
    task_params: Dict[str, Any],
    options: UpdateOptions
) -> int:
    """
    执行单个缺口下载任务
    
    Args:
        interface_name: 接口名称
        interface_config: 接口配置
        task_params: 任务参数（由 detect_stock_gaps 返回）
        options: 更新选项
        
    Returns:
        下载的记录数
    """
    ts_code = task_params.get('ts_code')
    
    logger.info(f"Downloading gap task for {ts_code}: {task_params}")
    
    # 构建请求参数
    params = {k: v for k, v in task_params.items() if not k.startswith('_')}
    
    # 执行请求
    try:
        data = self.downloader._make_request(interface_config, params)
        
        if data:
            # 保存数据
            self.storage_manager.save_data(interface_name, data, async_write=True)
            return len(data)
        
        return 0
        
    except Exception as e:
        logger.error(f"下载缺口任务失败 [{interface_name}/{ts_code}]: {e}")
        return 0
```

### 3.4 修改 `UpdateOptions` 模型

**文件**：[`app4/update/models.py`](app4/update/models.py)

**新增字段**：

```python
@dataclass
class UpdateOptions:
    """更新选项"""
    interfaces: List[str] = field(default_factory=list)
    groups: List[str] = field(default_factory=list)
    start_date: Optional[str] = None
    end_date: Optional[str] = None
    force: bool = False
    dry_run: bool = False
    gap_detection_enabled: bool = True
    min_gap_days: int = 1
    max_gaps: int = 50
    report_format: ReportFormat = ReportFormat.MARKDOWN
    report_file: Optional[str] = None
    ts_code: Optional[str] = None  # 新增：指定股票代码
```

### 3.5 修改 `run_update_mode()` 传递 ts_code

**文件**：[`app4/main.py`](app4/main.py)

**修改位置**：L539-548

```python
# 原代码
options = UpdateOptions(
    interfaces=valid_interfaces,
    start_date=args.start_date if user_provided_dates else None,
    end_date=args.end_date if user_provided_dates else None,
    force=getattr(args, 'update_force', False),
    dry_run=getattr(args, 'dry_run', False),
    gap_detection_enabled=True,
)

# 修改为
options = UpdateOptions(
    interfaces=valid_interfaces,
    start_date=args.start_date if user_provided_dates else None,
    end_date=args.end_date if user_provided_dates else None,
    force=getattr(args, 'update_force', False),
    dry_run=getattr(args, 'dry_run', False),
    gap_detection_enabled=True,
    ts_code=getattr(args, 'ts_code', None),  # 新增
)
```

---

## 四、测试验证方案

### 4.1 测试脚本

```bash
#!/bin/bash
# test_incremental_download.sh

PYTHON_PATH="/root/miniforge3/envs/get/bin/python"
MAIN_PY="/home/quan/testdata/aspipe_v4/app4/main.py"
DATA_DIR="/home/quan/testdata/aspipe_v4/data"

# 测试函数
test_interface() {
    local interface=$1
    local ts_code=$2
    
    echo "=========================================="
    echo "测试接口: $interface"
    echo "=========================================="
    
    # 清空数据
    rm -rf "${DATA_DIR}/${interface}"
    
    # 第一次下载（小范围）
    echo "[步骤1] 第一次下载（小范围）"
    $PYTHON_PATH $MAIN_PY --update --interface $interface --ts_code $ts_code \
        --start_date 20240101 --end_date 20240630
    
    # 第二次下载（大范围）
    echo "[步骤2] 第二次下载（大范围）"
    $PYTHON_PATH $MAIN_PY --update --interface $interface --ts_code $ts_code \
        --start_date 20230101 --end_date 20241231
    
    echo "------------------------------------------"
}

# 测试四种类型接口
test_interface "cyq_chips" "000001.SZ"        # 类型 A
test_interface "income_vip" "000001.SZ"       # 类型 B
test_interface "top10_holders" "000001.SZ"    # 类型 C
test_interface "pledge_detail" "000001.SZ"    # 类型 D
```

### 4.2 预期结果

| 接口类型 | 第一次下载 | 第二次下载预期行为 |
|---------|-----------|------------------|
| 类型 A | 下载指定范围数据 | 只下载缺失的交易日 |
| 类型 B | 下载指定范围数据 | 只下载缺失的报告期 |
| 类型 C | 下载指定范围数据 | 只下载缺失的锚点值 |
| 类型 D | 下载全部数据 | 跳过（已有数据） |

### 4.3 验证日志关键词

```
# 成功的缺口检测日志
[interface/ts_code] 缺口检测模式: trade_date/report_period/date_anchor
[ts_code] 缺失 N 个交易日/报告期/锚点值
[ts_code] Gap detection found N tasks to download

# 失败的日志（当前问题）
开始下载 interface: start_date ~ end_date
Downloaded N records for interface  # 全量下载
```

---

## 五、修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `app4/update/update_manager.py` | 修改 + 新增 | 修改 `update_interface()`，新增 `_update_with_stock_gap_detection()` 和 `_execute_gap_task()` |
| `app4/update/models.py` | 修改 | `UpdateOptions` 新增 `ts_code` 字段 |
| `app4/main.py` | 修改 | `run_update_mode()` 传递 `ts_code` 参数 |

---

## 六、风险评估

| 风险项 | 影响 | 缓解措施 |
|--------|------|---------|
| 修改核心下载逻辑 | 中等 | 保持原有逻辑作为回退路径 |
| 缺口检测性能 | 低 | 已有 LRU 缓存机制 |
| API 调用次数变化 | 低 | 缺口检测减少重复下载 |

---

## 七、实施步骤

1. **修改 `models.py`**：新增 `ts_code` 字段
2. **修改 `main.py`**：传递 `ts_code` 参数
3. **修改 `update_manager.py`**：
   - 修改 `update_interface()` 的缺口检测入口
   - 新增 `_update_with_stock_gap_detection()` 方法
   - 新增 `_execute_gap_task()` 方法
4. **运行测试脚本验证**
5. **检查日志确认增量下载生效**
