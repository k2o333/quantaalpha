# aspipe_v4 去重日志与存储流程优化实施方案

## 一、目标

1. 补充缺失的去重日志，提高系统可观测性
2. 优化小数据量场景的处理体验
3. 保持现有异步流水线架构的稳定性

---

## 二、具体实施步骤

### 步骤1: 补充批次内去重日志

**文件**: `app4/core/processor.py`

**修改位置**: `_remove_duplicates()` 方法 (第239-262行)

**当前代码**:
```python
def _remove_duplicates(self, df: pl.DataFrame, interface_config: Dict[str, Any]) -> pl.DataFrame:
    """批次内去重逻辑 - 根据主键去重，保留最后记录"""
    output_config = interface_config.get('output', {})
    primary_keys = output_config.get('primary_key', [])

    existing_keys = [key for key in primary_keys if key in df.columns]

    if existing_keys:
        if '_update_time' in df.columns:
            df = df.sort('_update_time', descending=False)
            df = df.unique(subset=existing_keys, keep='last')
        else:
            df = df.unique(subset=existing_keys, keep='last')

    # ... 排序逻辑 ...

    return df
```

**修改后代码**:
```python
def _remove_duplicates(self, df: pl.DataFrame, interface_config: Dict[str, Any]) -> pl.DataFrame:
    """批次内去重逻辑 - 根据主键去重，保留最后记录"""
    output_config = interface_config.get('output', {})
    primary_keys = output_config.get('primary_key', [])

    existing_keys = [key for key in primary_keys if key in df.columns]

    if existing_keys:
        original_count = len(df)

        if '_update_time' in df.columns:
            df = df.sort('_update_time', descending=False)
            df = df.unique(subset=existing_keys, keep='last')
        else:
            df = df.unique(subset=existing_keys, keep='last')

        removed_count = original_count - len(df)
        if removed_count > 0:
            logger.info(f"Batch deduplication for {interface_config.get('api_name', 'unknown')}: "
                       f"removed {removed_count} duplicates within batch, "
                       f"keys={existing_keys}")
        else:
            logger.debug(f"No duplicates found within batch for {interface_config.get('api_name', 'unknown')}")

    # ... 排序逻辑 ...

    return df
```

---

### 步骤2: 提升关键去重日志级别

**文件**: `app4/core/dedup.py`

**修改位置**: `deduplicate()` 方法 (第160-162行)

**当前代码**:
```python
if self.config['enable_stats']:
    self.logger.debug(f"Deduplication completed: {self.stats.input_rows} -> {self.stats.output_rows} rows "
                    f"({self.stats.get_dedup_rate():.2f}% dedup rate)")
```

**修改后代码**:
```python
if self.config['enable_stats']:
    # 使用 INFO 级别记录去重结果，提高可观测性
    log_level = logging.INFO if self.stats.removed_rows > 0 else logging.DEBUG
    self.logger.log(log_level, 
                   f"Deduplication completed: {self.stats.input_rows} -> {self.stats.output_rows} rows "
                   f"({self.stats.get_dedup_rate():.2f}% dedup rate)")
```

---

### 步骤3: 优化小数据量场景的处理体验

**文件**: `app4/core/storage.py`

**修改位置**: `add_to_buffer()` 方法 (第380-416行)

**策略**: 添加配置项支持小数据量立即处理模式

**当前代码**:
```python
def add_to_buffer(self, interface_name: str, data: List[Dict[str, Any]]) -> None:
    # ... 省略 ...
    with self.buffer_lock:
        # ... 省略 ...
        # Check if we should flush the buffer
        if buffer['count'] >= self.buffer_threshold:
            data_to_process = buffer['data']
            interface_to_process = interface_name
            buffer['data'] = []
            buffer['count'] = 0
    # ... 省略 ...
```

**修改后代码**:
```python
def add_to_buffer(self, interface_name: str, data: List[Dict[str, Any]], 
                  flush_immediately: bool = False) -> None:
    """
    Add data to buffer with optimized lock usage.

    Args:
        interface_name: 接口名称
        data: 数据列表
        flush_immediately: 是否立即刷新（适用于小数据量场景）
    """
    data_to_process = None
    interface_to_process = None

    with self.buffer_lock:
        buffer = self._get_or_create_buffer(interface_name)
        buffer['data'].extend(data)
        buffer['count'] += len(data)

        # Check if we should flush the buffer
        should_flush = (
            buffer['count'] >= self.buffer_threshold or
            flush_immediately or
            buffer['count'] < 100  # 小数据量立即处理
        )

        if should_flush:
            data_to_process = buffer['data']
            interface_to_process = interface_name
            buffer['data'] = []
            buffer['count'] = 0

    # Process the data outside the lock
    if data_to_process:
        item = {
            'interface': interface_to_process,
            'data': data_to_process,
            'timestamp': time.time()
        }
        self.process_queue.put(item)

        self.total_buffered_items = getattr(self, 'total_buffered_items', 0) + len(data_to_process)
        self.last_processed_time = time.time()

        if flush_immediately or len(data_to_process) < 100:
            logger.debug(f"Immediately processed {len(data_to_process)} records for {interface_to_process}")
```

---

### 步骤4: 优化日志输出顺序

**文件**: `app4/main.py`

**修改位置**: `finally` 块 (第728-752行)

**当前代码**:
```python
finally:
    logger.info("正在停止调度器...")
    if 'scheduler' in locals(): scheduler.stop()

    logger.info("正在关闭存储写入...")
    if 'storage_manager' in locals(): storage_manager.stop_writer()
```

**修改后代码**:
```python
finally:
    logger.info("正在停止调度器...")
    if 'scheduler' in locals(): scheduler.stop()

    logger.info("正在刷新并关闭存储写入...")
    if 'storage_manager' in locals():
        # 先显示缓存统计
        buffer_stats = storage_manager.get_buffer_stats()
        if buffer_stats['total_interfaces'] > 0:
            for iface, stats in buffer_stats['interface_stats'].items():
                if stats['buffer_count'] > 0:
                    logger.info(f"  - 接口 {iface}: {stats['buffer_count']} 条记录待写入")

        storage_manager.stop_writer()
```

---

### 步骤5: 添加去重统计报告

**文件**: `app4/core/storage.py`

**修改位置**: `_process_worker()` 方法 (第461-589行)

**在方法末尾添加统计信息**:

```python
def _process_worker(self):
    """处理线程：数据去重、验证、放入写入队列"""
    dedup_stats_total = {
        'total_processed': 0,
        'total_deduped': 0,
        'interfaces': set()
    }

    while self.running:
        try:
            # ... 原有逻辑 ...

            # ✅ 与历史数据去重（外部去重）
            if dedup_config.get('dedup_enabled', True) and primary_keys:
                # ... 原有去重逻辑 ...

                if not existing_df.is_empty():
                    # ... 原有逻辑 ...

                    df, dedup_stats = deduplicate_against_existing(...)

                    # 累加统计
                    dedup_stats_total['total_processed'] += dedup_stats.input_rows
                    dedup_stats_total['total_deduped'] += dedup_stats.removed_rows
                    dedup_stats_total['interfaces'].add(interface_name)

                    logger.info(f"Deduplication completed for {interface_name}: "
                               f"input={dedup_stats.input_rows}, "
                               f"output={dedup_stats.output_rows}, "
                               f"removed={dedup_stats.removed_rows}")

            # ... 原有逻辑 ...

        except queue.Empty:
            continue
        except Exception as e:
            # ... 原有错误处理 ...

    # 线程结束时输出统计
    if dedup_stats_total['total_processed'] > 0:
        dedup_rate = (dedup_stats_total['total_deduped'] / dedup_stats_total['total_processed']) * 100
        logger.info(f"Process worker summary: processed {dedup_stats_total['total_processed']} records, "
                   f"deduped {dedup_stats_total['total_deduped']} ({dedup_rate:.2f}%), "
                   f"interfaces={len(dedup_stats_total['interfaces'])}")
```

---

## 三、配置优化建议

### 3.1 添加小数据量模式配置

**文件**: `app4/config/settings.yaml`

**添加配置项**:
```yaml
storage:
  base_dir: "data"
  format: "parquet"
  batch_size: 10000
  # 新增：小数据量阈值，低于此值立即处理
  small_batch_threshold: 100
  # 新增：是否启用小数据量立即处理
  enable_small_batch_flush: true
```

### 3.2 日志级别配置

**文件**: `app4/config/settings.yaml`

**建议修改为**:
```yaml
logging:
  level: "INFO"
  file: "log/app4.log"
  max_size_mb: 100
  backup_count: 5
  # 新增：详细日志模式（调试用）
  verbose_dedup: true  # 启用详细去重日志
```

---

## 四、验证测试方案

### 4.1 测试用例1: 批次内去重日志

```bash
# 运行单只股票下载
python app4/main.py --interface income_vip --ts_code 000001.SZ --log-level DEBUG

# 验证日志中是否出现:
# - "Batch deduplication for income_vip: removed X duplicates within batch"
```

### 4.2 测试用例2: 与历史数据去重日志

```bash
# 第一次运行（创建历史数据）
python app4/main.py --interface income_vip --ts_code 000001.SZ

# 第二次运行（应该触发与历史数据去重）
python app4/main.py --interface income_vip --ts_code 000001.SZ

# 验证日志中是否出现:
# - "Deduplication completed for income_vip: input=X, output=Y, removed=Z"
```

### 4.3 测试用例3: 小数据量处理

```bash
# 测试小数据量是否立即处理
python app4/main.py --interface income_vip --ts_code 000001.SZ

# 验证日志中是否出现:
# - "Immediately processed X records for income_vip"
```

---

## 五、实施优先级

| 优先级 | 步骤 | 影响范围 | 工作量 |
|-------|------|---------|--------|
| P0 | 步骤1: 补充批次内去重日志 | `processor.py` | 小 |
| P0 | 步骤2: 提升去重日志级别 | `dedup.py` | 小 |
| P1 | 步骤4: 优化日志输出顺序 | `main.py` | 小 |
| P1 | 步骤5: 添加去重统计报告 | `storage.py` | 中 |
| P2 | 步骤3: 优化小数据量处理 | `storage.py` | 中 |
| P2 | 配置优化 | `settings.yaml` | 小 |

---

## 六、回滚方案

所有修改均为日志增强和体验优化，不涉及核心逻辑变更。如需回滚：

1. 使用 git 回滚到修改前的版本
2. 或直接注释掉新增的日志代码
3. 保持原有异步流水线架构不变

---

*方案制定时间: 2026-01-30*
*适用版本: aspipe_v4 app4*
