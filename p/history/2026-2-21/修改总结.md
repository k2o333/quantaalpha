# 修改总结 - 日期锚定接口参数传递修复

## 目标
修复 `cyq_perf` 接口（`reverse_date_range` + `is_date_anchor: true`）的参数传递问题

## 受影响接口
| 接口 | 模式 | 影响 |
|------|------|------|
| cyq_perf | reverse_date_range | **修复目标** |
| top10_holders 等 | stock_loop | 不受影响 |

## 修改文件清单

### 1. app4/core/params_builder.py
```
第 16 行：新增 DATE_ANCHOR_RANGE 场景枚举
第 66-73 行：build() 方法新增分支
第 107-118 行：_detect_scenario() 方法新增检测逻辑
第 207 行后：新增 _build_date_anchor_range_params() 方法
第 250-263 行：build_params_list() 方法新增分支
第 345 行后：新增 _build_date_anchor_range_params_list() 方法
第 345 行后：新增 _generate_daily_anchor_values() 方法
```

### 2. app4/core/pagination.py
```
第 9 行：确保已导入 timedelta
第 77-92 行：compose() 方法新增分支
第 186 行后：新增 _apply_date_anchor_range() 方法
第 186 行后：新增 _find_date_anchor_param() 方法
第 186 行后：新增 _generate_daily_dates() 方法
```

## 核心逻辑

```
检测条件：mode == 'reverse_date_range' AND is_date_anchor == true AND user_provided_dates

转换逻辑：
  输入：{start_date: "20260205", end_date: "20260220"}
  输出：[
    {trade_date: "20260220"},
    {trade_date: "20260219"},
    {trade_date: "20260218"},
    ...
  ]
```

## 测试命令
```bash
# 修复验证
python app4/main.py --update --interface cyq_perf --start_date 20260205 --end_date 20260220

# 不受影响验证
python app4/main.py --interface top10_holders --start_date 20260101 --end_date 20260220
python app4/main.py --interface daily --start_date 20260201 --end_date 20260205
```

## 风险等级
低 - 仅影响 cyq_perf 接口
