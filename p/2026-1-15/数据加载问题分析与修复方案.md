# 数据加载问题分析与修复方案

## 问题概述

运行 `app4/main.py` 时，发现以下三个问题：
1. 交易日历没有从本地 data 目录加载，每次都从 API 重新获取
2. 股票列表加载失败，报错 "data type mismatch for column list_date: incoming: Date != target: String"
3. income_vip 接口重复下载数据，虽然本地已存在

---

## 问题一：交易日历未从本地加载

### 现象
日志显示：
```
2026-01-18 10:45:25,307 - core.downloader - INFO - Trade calendar not found locally, fetching from API: 20240401-20240705
```

尽管 `/data/trade_cal/` 目录下存在多个交易日历文件，但程序没有加载它们。

### 根本原因

在 `main.py:199` 的 `preload_global_trade_calendar` 函数中：

```python
# 检查本地是否有数据
trade_calendar = downloader._get_trade_calendar_from_data_dir(start_date, end_date)

if trade_calendar:
     logger.info(f"Global trade calendar loaded from data directory: {len(trade_calendar)} trade days")
     # 手动填充内存缓存
     cache_key = (start_date, end_date)
     with downloader._cache_lock:
         downloader._memory_cache['trade_cal'][cache_key] = trade_calendar
     return trade_calendar
```

调用 `downloader._get_trade_calendar_from_data_dir(start_date, end_date)` 时，传递了具体的日期范围参数。但这个方法内部实现是：

1. 读取整个 `data/trade_cal/` 目录下的所有 parquet 文件
2. 在内存中过滤出指定日期范围的数据

**潜在问题**：
- 如果目录中存在多个文件，polars 在读取并合并时可能遇到 schema 不一致问题
- 过滤条件可能过于严格，导致没有数据匹配
- 文件命名格式与过滤逻辑不匹配

### 验证方法

在 `downloader.py` 的 `_get_trade_calendar_from_data_dir` 方法中添加详细日志：

```python
def _get_trade_calendar_from_data_dir(self, start_date, end_date):
    """从 Data 目录查询交易日历 (Source of Truth) - 优化版本"""
    logger.debug(f"_get_trade_calendar_from_data_dir called with {start_date}-{end_date}")

    storage_dir = self.global_config.get('storage', {}).get('base_dir', '../data')
    dir_path = os.path.join(storage_dir, 'trade_cal')
    logger.debug(f"Looking for trade calendar in: {dir_path}")

    if not os.path.exists(dir_path):
        logger.debug(f"Directory {dir_path} does not exist")
        return None

    try:
        # 读取目录下所有 parquet 文件 (Dataset 模式)
        all_files = [f for f in os.listdir(dir_path) if f.endswith('.parquet')]
        logger.debug(f"Found {len(all_files)} parquet files in {dir_path}")

        if not all_files:
            logger.debug("No parquet files found")
            return None

        # 尝试读取所有文件
        try:
            df = pl.read_parquet(dir_path)
            logger.debug(f"Successfully read all files, shape: {df.shape}")
            logger.debug(f"Columns: {df.columns}")
            logger.debug(f"Sample data: {df.head()}")
        except Exception as read_error:
            logger.warning(f"Failed to read parquet dataset: {read_error}")
            # 尝试逐个文件读取
            return self._read_trade_calendar_files_individually(dir_path, start_date, end_date)

        # ... 后续过滤逻辑
```

### 修复方案

**方案A：增强错误处理和日志（推荐）**

修改 `downloader.py` 中的 `_get_trade_calendar_from_data_dir` 方法：

```python
def _get_trade_calendar_from_data_dir(self, start_date, end_date):
    """从 Data 目录查询交易日历 - 增强版"""
    storage_dir = self.global_config.get('storage', {}).get('base_dir', '../data')
    dir_path = os.path.join(storage_dir, 'trade_cal')

    if not os.path.exists(dir_path):
        logger.debug(f"Trade calendar directory not found: {dir_path}")
        return None

    try:
        # 方法1：尝试作为Dataset读取（更高效）
        try:
            df = pl.read_parquet(dir_path)
            logger.debug(f"Successfully read trade calendar dataset, shape: {df.shape}")
        except Exception as e:
            logger.warning(f"Failed to read trade calendar as dataset: {e}")
            # 方法2：逐个文件读取
            df = self._read_trade_calendar_files_individually(dir_path)

        if df is None or df.is_empty():
            logger.debug("No trade calendar data found")
            return None

        # 构建过滤条件 - 更健壮的实现
        conditions = []

        # 确保 cal_date 列存在
        if 'cal_date' not in df.columns:
            logger.warning(f"cal_date column not found in trade calendar data. Available columns: {df.columns}")
            return None

        # 日期范围过滤
        conditions.append(pl.col('cal_date') >= start_date)
        conditions.append(pl.col('cal_date') <= end_date)

        # 交易日过滤
        if 'is_open' in df.columns:
            conditions.append(pl.col('is_open') == 1)
        else:
            logger.warning("is_open column not found, skipping trade day filter")

        # 交易所过滤（如果存在该列）
        if 'exchange' in df.columns:
            conditions.append(pl.col('exchange') == 'SSE')

        # 应用过滤
        filtered_df = df.filter(pl.all_horizontal(conditions))

        # 去重和排序
        if not filtered_df.is_empty():
            # 按 cal_date 去重，保留最新的
            filtered_df = filtered_df.unique(subset=['cal_date'], keep='last')
            # 按日期排序
            filtered_df = filtered_df.sort('cal_date')

            logger.info(f"Loaded {len(filtered_df)} trade days from local storage ({start_date} to {end_date})")
            return filtered_df.to_dicts()
        else:
            logger.debug(f"No trade days found in range {start_date}-{end_date}")
            return None

    except Exception as e:
        logger.warning(f"Failed to read trade calendar from Data dir: {e}")
        import traceback
        logger.debug(f"Full traceback: {traceback.format_exc()}")
        return None

def _read_trade_calendar_files_individually(self, dir_path):
    """逐个读取交易日历文件"""
    all_files = [f for f in os.listdir(dir_path) if f.endswith('.parquet')]
    all_data = []

    for file_name in all_files:
        file_path = os.path.join(dir_path, file_name)
        try:
            df = pl.read_parquet(file_path)
            if not df.is_empty():
                all_data.append(df)
                logger.debug(f"Read {len(df)} rows from {file_name}")
        except Exception as e:
            logger.warning(f"Failed to read {file_path}: {e}")
            continue

    if not all_data:
        return None

    # 合并所有数据
    if len(all_data) == 1:
        return all_data[0]
    else:
        try:
            # 尝试正常合并
            return pl.concat(all_data, how='vertical')
        except Exception as e:
            logger.warning(f"Failed to vertically concat data, trying diagonal: {e}")
            # 如果schema不一致，使用diagonal模式
            return pl.concat(all_data, how='diagonal')
```

**方案B：简化方案（快速验证）**

直接在 `main.py` 中绕过复杂的本地加载逻辑，使用 `storage_manager` 读取：

```python
def preload_global_trade_calendar(downloader, storage_manager, start_date='19900101', end_date=None):
    """预加载全局交易日历"""
    if end_date is None:
        from datetime import datetime
        end_date = datetime.now().strftime('%Y%m%d')

    logger.info(f"Preloading global trade calendar: {start_date} - {end_date}")

    # 尝试从存储管理器读取
    try:
        df = storage_manager.read_interface_data('trade_cal', start_date, end_date)
        if not df.is_empty():
            trade_calendar = df.to_dicts()
            logger.info(f"Loaded {len(trade_calendar)} trade days from storage")

            # 更新内存缓存
            cache_key = (start_date, end_date)
            with downloader._cache_lock:
                downloader._memory_cache['trade_cal'][cache_key] = trade_calendar

            return trade_calendar
    except Exception as e:
        logger.debug(f"Failed to load from storage manager: {e}")

    # 回退到原逻辑
    # ... 原有代码
```

---

## 问题二：股票列表 schema 不匹配

### 现象

日志显示：
```
2026-01-18 10:45:24,577 - core.downloader - WARNING - Failed to read stock list from Data dir: data type mismatch for column list_date: incoming: Date != target: String
```

### 根本原因

在 `downloader.py:504-523` 的 `_get_stock_list_from_data_dir` 方法中：

```python
def _get_stock_list_from_data_dir(self):
    """从Data目录获取股票列表"""
    try:
        storage_dir = self.global_config.get('storage', {}).get('base_dir', '../data')
        dir_path = os.path.join(storage_dir, 'stock_basic')

        if not os.path.exists(dir_path):
            return None

        # 读取目录下所有 parquet 文件
        df = pl.read_parquet(dir_path)  # <-- 这里报错

        if df.is_empty():
            return None

        return df.to_dicts()

    except Exception as e:
        logger.warning(f"Failed to read stock list from Data dir: {e}")
        return None
```

**问题分析**：
- `data/stock_basic/` 目录下有多个 parquet 文件
- 这些文件的 schema 不一致：有些文件的 `list_date` 列是 Date 类型，有些是 String 类型
- 当 `pl.read_parquet(dir_path)` 尝试读取整个目录并合并时，polars 无法自动处理这种类型不匹配

### 验证方法

检查现有文件的 schema：

```bash
# 检查新旧文件的schema差异
python3 -c "
import polars as pl
import os

# 读取一个旧文件
old_file = 'data/stock_basic/stock_basic_nodate_1767963976743_e18e89db.parquet'
if os.path.exists(old_file):
    df_old = pl.read_parquet(old_file)
    print('Old file schema:')
    print(df_old.schema)

# 读取一个新文件
import glob
files = glob.glob('data/stock_basic/*.parquet')
if files:
    df_new = pl.read_parquet(files[-1])
    print('\\nNew file schema:')
    print(df_new.schema)
"
```

### 修复方案

**方案：逐个文件读取并统一 schema**

修改 `downloader.py` 中的 `_get_stock_list_from_data_dir` 方法：

```python
def _get_stock_list_from_data_dir(self) -> Optional[List[Dict[str, Any]]]:
    """从Data目录获取股票列表 - 修复schema不一致问题"""
    try:
        storage_dir = self.global_config.get('storage', {}).get('base_dir', '../data')
        dir_path = os.path.join(storage_dir, 'stock_basic')

        if not os.path.exists(dir_path):
            logger.debug(f"Stock basic directory not found: {dir_path}")
            return None

        # 读取目录下所有 parquet 文件，处理schema不一致问题
        all_files = [f for f in os.listdir(dir_path) if f.endswith('.parquet')]
        if not all_files:
            logger.debug("No stock basic files found")
            return None

        logger.debug(f"Found {len(all_files)} stock basic files")

        all_data = []
        for file_name in all_files:
            file_path = os.path.join(dir_path, file_name)
            try:
                df = pl.read_parquet(file_path)
                if not df.is_empty():
                    # 确保 list_date 是字符串类型
                    if 'list_date' in df.columns:
                        # 检查并转换类型
                        if df.schema['list_date'] != pl.Utf8:
                            logger.debug(f"Converting list_date to string in {file_name}")
                            df = df.with_columns([
                                pl.col('list_date').cast(pl.Utf8).alias('list_date')
                            ])

                    all_data.append(df)
                    logger.debug(f"Successfully read {len(df)} rows from {file_name}")
            except Exception as e:
                logger.warning(f"Failed to read {file_path}: {e}")
                continue

        if not all_data:
            logger.debug("No valid stock basic data found")
            return None

        # 合并所有数据
        if len(all_data) == 1:
            df = all_data[0]
        else:
            try:
                # 尝试垂直合并（要求schema一致）
                df = pl.concat(all_data, how='vertical')
            except Exception as e:
                logger.warning(f"Failed to vertically concat data, trying diagonal: {e}")
                # 如果schema不完全一致，使用diagonal模式
                df = pl.concat(all_data, how='diagonal')

        if df.is_empty():
            logger.debug("Combined DataFrame is empty")
            return None

        # 去重，保留最新的记录（基于 _update_time 或文件名中的时间戳）
        if 'ts_code' in df.columns:
            if '_update_time' in df.columns:
                df = df.sort('_update_time', descending=True)
            df = df.unique(subset=['ts_code'], keep='first')  # 保留最新的
            logger.info(f"Loaded {len(df)} unique stocks from local storage")
        else:
            logger.warning("ts_code column not found, skipping deduplication")

        return df.to_dicts()

    except Exception as e:
        logger.warning(f"Failed to read stock list from Data dir: {e}")
        import traceback
        logger.debug(f"Full traceback: {traceback.format_exc()}")
        return None
```

**替代方案：在存储时统一 schema**

如果问题根源是存储时 schema 不统一，可以在 `storage.py` 的 `_write_interface_data` 方法中添加 schema 统一逻辑：

```python
def _write_interface_data(self, interface_name: str, data: List[Dict[str, Any]]):
    """写入接口数据 - Parquet Dataset 模式"""
    # ... 前面代码 ...

    df = pl.DataFrame(data)

    # Schema 统一处理
    if interface_name == 'stock_basic':
        # 确保 list_date 是字符串类型
        if 'list_date' in df.columns:
            df = df.with_columns([
                pl.col('list_date').cast(pl.Utf8).alias('list_date')
            ])

    # ... 后续代码 ...
```

---

## 问题三：income_vip 重复下载

### 现象

日志显示：
```
2026-01-18 10:45:25,463 - core.downloader - INFO - Coverage decision for income_vip: Coverage 0.00% too low, downloading full range
2026-01-18 10:45:26,067 - core.downloader - INFO - Downloaded 2 records for date range 20240401-20240705
...
2026-01-18 10:45:26,239 - core.storage - INFO - All 1 records already exist for income_vip, skipping save
```

虽然下载了数据，但最后发现数据已存在，没有保存。这意味着覆盖率检测没有正确识别已存在的数据。

### 根本原因

`coverage_manager.py` 的 `_check_range_coverage` 方法（96-174行）负责检测数据覆盖率：

```python
def _check_range_coverage(self, interface_name: str, params: Dict[str, Any]) -> bool:
    """检查日期范围覆盖率"""
    start_date = params.get('start_date')
    end_date = params.get('end_date')

    # ... 参数检查 ...

    try:
        # 读取接口数据，只读取日期列
        df = self.storage_manager.read_interface_data(
            interface_name,
            start_date=start_date,
            end_date=end_date,
            columns=[date_column],
            **{k: v for k, v in params.items() if k not in ['start_date', 'end_date']}
        )

        if df.is_empty():
            logger.debug(f"No existing data found for {interface_name} in range {start_date}-{end_date}")
            return False

        # 获取实际存在的日期
        actual_dates_raw = df[date_column].to_list()
        # ... 处理日期格式 ...

        # 获取交易日历
        if self.downloader:
            trade_calendar = self.downloader.get_trade_calendar(start_date, end_date)
        else:
            # ...

        # 计算覆盖率
        expected_dates = {day['cal_date'] for day in trade_calendar if day.get('is_open', 0) == 1}
        coverage = len(actual_dates & expected_dates) / len(expected_dates)

        result = coverage >= threshold
        return result

    except Exception as e:
        logger.warning(f"Range coverage check failed for {interface_name}: {e}")
        return False
```

**问题分析**：

1. **schema 问题**：`storage_manager.read_interface_data` 在读取 `income_vip` 数据时，可能遇到 schema 不一致，导致读取失败或返回空数据
2. **日期列识别问题**：对于 `income_vip` 接口，日期列应该是 `period` 而不是 `trade_date`
3. **参数传递问题**：在读取现有数据时，可能没有正确传递 `ts_code` 等参数，导致读取到的数据不匹配

查看 `data/income_vip/` 目录下的文件：
```
income_vip_20231231_20231231_1768619727820_3e492c72.parquet
income_vip_20240331_20240331_1768621108376_c85dbb13.parquet
```

这些文件名显示数据是按 `period` 存储的（季度末日期）。

### 验证方法

在 `coverage_manager.py` 中添加详细日志：

```python
def _check_range_coverage(self, interface_name: str, params: Dict[str, Any]) -> bool:
    """检查日期范围覆盖率 - 调试版"""
    start_date = params.get('start_date')
    end_date = params.get('end_date')

    logger.debug(f"Checking range coverage for {interface_name}: {start_date}-{end_date}")
    logger.debug(f"Params: {params}")

    # 获取接口配置
    interface_config = self.config_loader.get_interface_config(interface_name)
    detection_config = interface_config.get('duplicate_detection', {})

    # 获取检测列
    date_column = detection_config.get('date_column', 'trade_date')
    logger.debug(f"Using date column: {date_column}")

    # 读取现有数据
    read_params = {k: v for k, v in params.items() if k not in ['start_date', 'end_date']}
    logger.debug(f"Read params: {read_params}")

    df = self.storage_manager.read_interface_data(
        interface_name,
        start_date=start_date,
        end_date=end_date,
        columns=[date_column],
        **read_params
    )

    logger.debug(f"Existing data shape: {df.shape if not df.is_empty() else 'empty'}")
    if not df.is_empty():
        logger.debug(f"Sample existing data: {df.head()}")
        logger.debug(f"Existing date range: {df[date_column].min()} to {df[date_column].max()}")

    # ... 后续逻辑
```

### 修复方案

**方案A：配置驱动的日期列识别**

修改 `income_vip.yaml` 配置文件，明确指定日期列：

```yaml
# config/interfaces/income_vip.yaml
api_name: income_vip
description: 业绩快报
duplicate_detection:
  enabled: true
  date_column: period  # 明确指定使用 period 列
  threshold: 0.95
  key_column: period   # 对于 period_range 模式，使用 period 作为主键
pagination:
  enabled: true
  mode: period_range
  # ... 其他配置 ...
```

**方案B：增强 coverage_manager 的健壮性**

修改 `coverage_manager.py` 的 `_check_range_coverage` 方法：

```python
def _check_range_coverage(self, interface_name: str, params: Dict[str, Any]) -> bool:
    """检查日期范围覆盖率 - 增强版"""
    start_date = params.get('start_date')
    end_date = params.get('end_date')

    if not start_date or not end_date:
        logger.debug(f"Missing date range parameters for {interface_name}, skipping coverage check")
        return False

    # 获取接口配置
    interface_config = self.config_loader.get_interface_config(interface_name)
    detection_config = interface_config.get('duplicate_detection', {})

    # 智能识别日期列
    # 对于 period_range 模式，优先使用 period 列
    pagination_config = interface_config.get('pagination', {})
    pagination_mode = pagination_config.get('mode', 'offset') if pagination_config.get('enabled', False) else 'none'

    if pagination_mode == 'period_range' and 'period' not in detection_config:
        date_column = 'period'  # 自动识别为 period
        logger.debug(f"Auto-detected date column 'period' for period_range mode")
    else:
        date_column = detection_config.get('date_column', 'trade_date')

    threshold = detection_config.get('threshold', 0.95)
    logger.debug(f"Checking coverage for {interface_name} using column {date_column}")

    try:
        # 读取接口数据，只读取日期列
        read_params = {k: v for k, v in params.items() if k not in ['start_date', 'end_date']}

        # 对于 period_range 接口，不使用日期范围过滤，而是读取所有数据
        if pagination_mode == 'period_range':
            df = self.storage_manager.read_interface_data(
                interface_name,
                columns=[date_column],
                **read_params
            )
        else:
            df = self.storage_manager.read_interface_data(
                interface_name,
                start_date=start_date,
                end_date=end_date,
                columns=[date_column],
                **read_params
            )

        if df.is_empty():
            logger.debug(f"No existing data found for {interface_name}")
            return False

        # 获取实际存在的日期，并统一转换为字符串格式
        actual_dates_raw = df[date_column].to_list()
        actual_dates = set()
        for date_val in actual_dates_raw:
            if isinstance(date_val, str):
                actual_dates.add(date_val)
            elif hasattr(date_val, 'strftime'):  # date or datetime object
                actual_dates.add(date_val.strftime('%Y%m%d'))
            else:
                actual_dates.add(str(date_val))

        logger.debug(f"Found {len(actual_dates)} existing dates for {interface_name}")

        # 获取交易日历（或生成预期日期范围）
        if pagination_mode == 'period_range':
            # 对于 period_range，直接生成季度末日期
            expected_dates = set(self._generate_quarter_end_dates(start_date, end_date))
            logger.debug(f"Generated {len(expected_dates)} expected quarter-end dates")
        else:
            # 对于普通日期范围，使用交易日历
            if self.downloader:
                trade_calendar = self.downloader.get_trade_calendar(start_date, end_date)
            else:
                logger.warning("Downloader not available for trade calendar check")
                return not df.is_empty()

            if not trade_calendar:
                logger.info(f"Trade calendar not available for {interface_name}, using simple coverage check")
                return not df.is_empty()

            # 过滤出交易日
            expected_dates = {day['cal_date'] for day in trade_calendar if day.get('is_open', 0) == 1}

        if not expected_dates:
            logger.warning(f"No expected dates in range {start_date}-{end_date}")
            return False

        # 计算覆盖率
        covered_dates = actual_dates & expected_dates
        coverage = len(covered_dates) / len(expected_dates) if expected_dates else 0.0

        logger.info(f"Coverage for {interface_name} ({start_date}-{end_date}): {coverage:.2%} "
                   f"({len(covered_dates)}/{len(expected_dates)})")
        logger.debug(f"Covered dates: {sorted(list(covered_dates))[:5]}...")  # 只显示前5个
        logger.debug(f"Missing dates: {sorted(list(expected_dates - actual_dates))[:5]}...")

        result = coverage >= threshold
        return result

    except Exception as e:
        logger.warning(f"Range coverage check failed for {interface_name}: {e}")
        import traceback
        logger.debug(f"Full traceback: {traceback.format_exc()}")
        return False

def _generate_quarter_end_dates(self, start_date: str, end_date: str) -> List[str]:
    """生成季度末日期列表"""
    from datetime import datetime

    start_dt = datetime.strptime(start_date, '%Y%m%d')
    end_dt = datetime.strptime(end_date, '%Y%m%d')

    quarter_ends = []
    current = start_dt

    # 移动到下一个季度末
    while current <= end_dt:
        year = current.year

        # 四个季度末
        for month, day in [(3, 31), (6, 30), (9, 30), (12, 31)]:
            qe_date = datetime(year, month, day)
            if start_dt <= qe_date <= end_dt:
                quarter_ends.append(qe_date.strftime('%Y%m%d'))

        current = datetime(year + 1, 1, 1)

    return quarter_ends
```

**方案C：在 storage_manager 中添加 schema 处理**

修改 `storage.py` 的 `read_interface_data` 方法，处理 schema 不一致：

```python
def read_interface_data(self, interface_name: str, start_date: str = None, end_date: str = None,
                       columns: Optional[List[str]] = None, **kwargs) -> pl.DataFrame:
    """读取接口数据 - 支持文件名过滤和确定性去重"""
    dir_path = os.path.join(self.storage_dir, interface_name)

    if not os.path.exists(dir_path):
        return pl.DataFrame()

    try:
        # 文件名过滤
        files_to_read = []
        all_files = os.listdir(dir_path)

        for f in all_files:
            if not f.endswith('.parquet'):
                continue

            # 文件名格式: name_start_end_ts_uuid.parquet
            parts = f.split('_')

            # 基于日期范围过滤文件
            if len(parts) >= 4 and start_date and end_date and parts[1] != "nodate":
                try:
                    f_start, f_end = parts[1], parts[2]

                    # 检查文件日期范围是否与请求范围重叠
                    if f_end >= start_date and f_start <= end_date:
                        files_to_read.append(os.path.join(dir_path, f))
                    else:
                        logger.debug(f"Skipping file {f} (range {f_start}-{f_end} doesn't overlap with {start_date}-{end_date})")
                except:
                    # 如果解析失败，包含该文件
                    files_to_read.append(os.path.join(dir_path, f))
            else:
                files_to_read.append(os.path.join(dir_path, f))

        if not files_to_read:
            logger.debug(f"No files to read for {interface_name} in range {start_date}-{end_date}")
            return pl.DataFrame()

        logger.debug(f"Reading {len(files_to_read)} files for {interface_name}")

        # 逐个文件读取，处理schema不一致
        dataframes = []
        for file_path in files_to_read:
            try:
                df_file = pl.read_parquet(file_path)
                if not df_file.is_empty():
                    dataframes.append(df_file)
                    logger.debug(f"Read {len(df_file)} rows from {os.path.basename(file_path)}")
            except Exception as e:
                logger.warning(f"Failed to read {file_path}: {e}")
                continue

        if not dataframes:
            return pl.DataFrame()

        # 合并数据
        if len(dataframes) == 1:
            df = dataframes[0]
        else:
            try:
                # 尝试垂直合并
                df = pl.concat(dataframes, how='vertical')
            except Exception as e:
                logger.warning(f"Failed to vertically concat data for {interface_name}: {e}")
                # 使用diagonal模式
                df = pl.concat(dataframes, how='diagonal')

        logger.debug(f"Combined DataFrame shape: {df.shape}")

        # 过滤额外参数
        for param_name, param_value in kwargs.items():
            if param_name in df.columns and param_value is not None:
                df = df.filter(pl.col(param_name) == param_value)
                logger.debug(f"Filtered by {param_name}={param_value}, new shape: {df.shape}")

        # 确定性去重
        interface_config = self._get_interface_config(interface_name)
        primary_keys = interface_config.get('output', {}).get('primary_key', [])

        if primary_keys and not df.is_empty():
            existing_keys = [k for k in primary_keys if k in df.columns]
            if existing_keys:
                if '_update_time' in df.columns:
                    df = df.sort('_update_time', descending=False)

                original_len = len(df)
                df = df.unique(subset=existing_keys, keep='last')
                logger.debug(f"Deduplicated {original_len - len(df)} records, kept {len(df)}")

        return df

    except Exception as e:
        logger.error(f"Error reading interface data for {interface_name}: {e}")
        import traceback
        logger.debug(f"Full traceback: {traceback.format_exc()}")
        return pl.DataFrame()
```

---

## 实施步骤

### 步骤 1：添加详细日志

在不修改核心逻辑的前提下，先添加详细日志以确认问题：

1. 修改 `downloader.py` 的 `_get_trade_calendar_from_data_dir` 和 `_get_stock_list_from_data_dir` 方法
2. 修改 `coverage_manager.py` 的 `_check_range_coverage` 方法
3. 修改 `storage.py` 的 `read_interface_data` 方法

### 步骤 2：应用修复

根据日志分析结果，应用相应的修复方案：

1. **交易日历问题**：使用方案A（增强错误处理）
2. **股票列表问题**：使用逐个文件读取方案
3. **重复下载问题**：使用方案B（增强 coverage_manager）或方案C（增强 storage_manager）

### 步骤 3：测试验证

1. 清理现有数据（可选）：
   ```bash
   # 备份现有数据
   cp -r data data.backup

   # 删除特定目录测试
   rm -rf data/trade_cal/* data/stock_basic/* data/income_vip/*
   ```

2. 运行测试：
   ```bash
   # 第一次运行 - 应该从API下载并保存
   python app4/main.py --interface income_vip --ts_code 000002.SZ --start_date 20240401 --end_date 20240705

   # 第二次运行 - 应该从本地加载，不重复下载
   python app4/main.py --interface income_vip --ts_code 000002.SZ --start_date 20240401 --end_date 20240705
   ```

3. 验证日志：
   - 第一次运行应该显示 "Downloading from API"
   - 第二次运行应该显示 "Loading from local storage" 和 "Skipping download (already exists)"

### 步骤 4：监控指标

关注以下日志关键字：
- `Trade calendar loaded from data directory`
- `Loaded X stocks from local storage`
- `Coverage for income_vip: X.XX%`
- `Skipping download (already exists)`
- `All X records already exist, skipping save`

---

## 预期效果

修复后，程序应该：

1. **交易日历**：优先从 `data/trade_cal/` 加载，API 作为备用
2. **股票列表**：成功读取 `data/stock_basic/` 下的所有文件，不受 schema 不一致影响
3. **数据下载**：在下载前正确检测数据是否已存在，避免重复下载和保存

### 性能提升

- 减少 API 调用次数（交易日历、股票列表）
- 避免重复数据下载
- 提高程序启动和运行速度

---

## 附录：相关代码位置

### 交易日历加载
- `app4/main.py:182-240` - `preload_global_trade_calendar`
- `app4/core/downloader.py:525-564` - `get_trade_calendar`
- `app4/core/downloader.py:566-609` - `_get_trade_calendar_from_data_dir`

### 股票列表加载
- `app4/main.py:458-470` - 股票列表获取逻辑
- `app4/core/downloader.py:504-523` - `_get_stock_list_from_data_dir`

### 覆盖率检测
- `app4/core/coverage_manager.py:96-174` - `_check_range_coverage`
- `app4/core/coverage_manager.py:274-283` - `should_skip`
- `app4/core/downloader.py:305-442` - 覆盖率决策逻辑

### 数据存储与读取
- `app4/core/storage.py:133-227` - `read_interface_data`
- `app4/core/storage.py:309-330` - `save_data_with_dedup`
