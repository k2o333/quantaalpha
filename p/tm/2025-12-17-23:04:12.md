# 修复TuShare API超时和数据不存在错误方案（简化版）

## 问题概述

根据日志分析，系统存在以下主要问题：

1. **超时错误**: `HTTPConnectionPool(host='tushare.xyz', port=5000): Read timed out. (read timeout=30)`
2. **数据不存在错误**: `指定数据不存在，请确认参数！`
3. **错误处理不当**: 现有代码没有正确区分网络错误和数据不存在错误

## 当前代码问题分析

### 1. download_with_pagination 方法的问题

当前实现（tushare_api.py:204-222）：
- 只处理了频率限制错误，没有处理网络超时和数据不存在错误
- 对所有非频率限制错误都直接退出，没有区分重试策略

### 2. cyq_chips 方法的问题

当前实现（tushare_api.py:334-337）：
- 单次请求量过大（limit_per_call=2000），容易导致超时
- 没有针对数据不存在错误的特殊处理

### 3. handle_api_error 方法的问题

当前实现（error_handler.py:60）：
- 将数据不存在错误和其他错误一样处理（raise error）
- 没有区分错误类型进行相应处理

## 推荐的简化修复方案

### 1. 修复 download_with_pagination 方法（tushare_api.py:175-244）

```python
def download_with_pagination(self, api_func, limit_per_call=2000, **base_kwargs):
    """
    分页下载数据的通用函数，改进错误处理
    """
    all_data = []
    offset = 0
    max_retries = 3  # 仅对网络错误重试
    retry_count = 0

    while True:
        # 添加分页参数
        kwargs = base_kwargs.copy()
        kwargs['offset'] = offset
        kwargs['limit'] = limit_per_call

        try:
            # 实现API调用前的频率限制
            api_name = getattr(api_func, '__name__', 'unknown_api')
            self._rate_limit(api_name)
            data = api_func(**kwargs)
            retry_count = 0  # 成功则重置重试计数
        except Exception as e:
            error_msg = str(e).lower()
            self.logger.error(f"分页下载失败, offset={offset}: {e}")

            # 检查错误类型并决定是否重试
            if "指定数据不存在" in str(e) or "data does not exist" in error_msg:
                # 数据不存在错误，不应该重试，直接结束
                self.logger.info(f"数据不存在，结束下载: {e}")
                break
            elif any(keyword in error_msg for keyword in ["timeout", "connection", "tushare.xyz", "network"]):
                # 网络相关错误，可以重试
                retry_count += 1
                if retry_count <= max_retries:
                    wait_time = min(10 * retry_count, 60)  # 指数退避，最大60秒
                    self.logger.warning(f"网络错误，等待 {wait_time:.2f} 秒后重试 (第{retry_count}/{max_retries}次)")
                    time.sleep(wait_time)
                    continue
                else:
                    self.logger.error(f"网络错误超过最大重试次数，跳过当前分页")
                    break
            else:
                # 其他错误，不重试
                self.logger.error(f"非网络错误，停止下载: {e}")
                break

        if data is None or len(data) == 0:
            break

        # 将DataFrame添加到列表中
        all_data.append(data)

        # 如果返回数据少于限制数量，说明已到最后一页
        if len(data) < limit_per_call:
            break

        # 更新offset
        offset += limit_per_call

        # 在每次API调用之间添加延迟
        time.sleep(random.uniform(0.5, 1.0))

    # 合并所有数据
    if all_data:
        return pd.concat(all_data, ignore_index=True)
    else:
        return pd.DataFrame()
```

### 2. 修改 download_cyq_chips_paginated 方法（tushare_api.py:292-342）

```python
def download_cyq_chips_paginated(self, trade_date: str = None, ts_code: str = None):
    """
    分页下载cyq_chips数据，改进错误处理
    """
    try:
        kwargs = {}
        if trade_date:
            kwargs['trade_date'] = trade_date
        if ts_code:
            kwargs['ts_code'] = ts_code

        # 减小每次调用的数据量，降低超时风险
        return self.download_with_pagination(
            self.pro.cyq_chips,
            limit_per_call=1000,  # 从2000减少到1000
            **kwargs
        )
    except Exception as e:
        self.logger.error(f"分页下载cyq_chips失败: {e}")

        # 对于cyq_chips特有的错误处理
        if "指定数据不存在" in str(e):
            self.logger.info("cyq_chips: 指定日期无数据，返回空DataFrame")
            return pd.DataFrame()
        else:
            # 其他错误重新抛出
            raise e
```

### 3. 增强 handle_api_error 方法（error_handler.py:46-66）

```python
@staticmethod
def handle_api_error(error: Exception, context: str = ""):
    """
    增强的API错误处理，区分错误类型
    """
    error_msg = str(error).lower()

    if "limit" in error_msg or "频次" in error_msg or "frequency" in error_msg:
        logger.error(f"API频率限制超出 in {context}: {error}")
        time.sleep(120)  # 频率限制等待更长时间
    elif "token" in error_msg or "auth" in error_msg:
        logger.error(f"认证错误 in {context}: {error}")
        raise error
    elif any(keyword in error_msg for keyword in ["network", "timeout", "connection", "tushare.xyz"]):
        logger.warning(f"网络错误 in {context}: {error}")
        time.sleep(30)  # 网络错误等待30秒后重试
    elif "指定数据不存在" in str(error):
        logger.warning(f"数据不存在 in {context}: {error}")
        # 数据不存在错误不需要抛出异常，由调用方处理
        pass
    else:
        logger.error(f"未知错误 in {context}: {error}")

    # 对于数据不存在错误，不抛出异常
    if "指定数据不存在" not in str(error):
        raise error
```

## 主要改进点

1. **区分错误类型**: 明确区分哪些错误应该重试（网络相关），哪些不应该（数据不存在）
2. **简化重试逻辑**: 只对网络错误进行重试，避免无意义的重试
3. **降低单次请求量**: 将cyq_chips的单次请求量从2000减少到1000，降低超时风险
4. **更合理的等待时间**: 根据错误类型设置不同的等待时间
5. **避免过度复杂**: 简化代码，只修复核心问题

## 实施步骤

1. 按上述修改更新`/home/quan/testdata/aspipe_v4/app/tushare_api.py`中的`download_with_pagination`和`download_cyq_chips_paginated`方法
2. 按上述修改更新`/home/quan/testdata/aspipe_v4/app/error_handler.py`中的`handle_api_error`方法
3. 测试更新后的代码
4. 监控修复效果

## 预期效果

1. **减少超时错误**: 通过降低单次请求量和改进网络错误处理
2. **改善错误处理**: 正确区分和处理不同类型的错误
3. **提高稳定性**: 避免对数据不存在错误进行无意义的重试
4. **更简洁的代码**: 避免过度复杂化，专注于解决核心问题

这个简化方案比原始建议更实用，能有效解决当前的问题，同时避免过度复杂化代码。