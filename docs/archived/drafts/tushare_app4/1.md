App4 代码优化实施计划
基于 
docs/archived/drafts/tushare_app4/optimization_review.md
 中的分析结论，对 
app4/core/processor.py
、
app4/core/pagination_executor.py
 和 
app4/update/update_manager.py
 三个文件进行针对性修改。

优先修复顺序（依报告建议）
P0 紧急 → P0 性能 → P1 健壮性

Proposed Changes
Component 1: 
app4/core/pagination_executor.py
[MODIFY] 
pagination_executor.py
问题3.1 — 5处裸 except: 替换

全文共存在以下裸捕获（行号来自当前文件）：

第619行：
_should_skip_by_coverage
 第一次 coverage_manager.should_skip() 的 except:
第655行：
_should_skip_by_coverage
 第二次（主路径）except:
第668行（死代码区）：except:（连同死代码一起删除）
第716行（死代码区）：except:（连同死代码一起删除）
第736行：
_estimate_empty_days
 中的 except:
替换方式：全部改为 except Exception as e:，即使是 return False 的情况也加 logger.debug(f"Coverage check failed: {e}")。

问题3.2 — 删除 
_should_skip_by_coverage
 中的死代码块

第 656-717 行（try: return coverage_manager.should_skip... 之后的整个重复代码块） 属于永远不可达的死代码（其上方第 651-655 行已经 return 或 raise），直接删除。

问题3.3 — 
_estimate_empty_days
 静默错误修复

python
# 当前（有问题）
except:
    pass
return 1
# 修复后
except ValueError as e:
    logger.warning(f"_estimate_empty_days: 日期解析失败 {params.get('_time_window')!r}: {e}，默认返回1天")
return 1
Component 2: 
app4/core/processor.py
[MODIFY] 
processor.py
问题1.1 — 
_handle_primary_keys
 的 Polars化

当前问题：第208行 data_list = df.to_dicts() 强制将整个 DataFrame 序列化为 Python dict，然后调用 
_detect_duplicates_fast
（纯Python set循环）做重复检测。

修改方案：

删除 data_list = df.to_dicts() 以及后续 detection_result = self._detect_duplicates_fast(...) 调用
改用 Polars 原生 df.is_duplicated() 进行重复计数报警：
python
if primary_keys:
    existing_keys = [k for k in primary_keys if k in df.columns]
    if existing_keys:
        dup_count = df.filter(df.is_duplicated()).height
        if dup_count > 0:
            logger.warning(f"Found {dup_count} duplicate records for interface {interface_name}")
Schema应用部分改为直接在 df 上做 cast，而不重建 pl.DataFrame(data_list, schema=...)：
python
predefined_schema = SchemaManager.load_schema(interface_name)
if predefined_schema:
    try:
        cast_exprs = [pl.col(c).cast(t, strict=False) for c, t in predefined_schema.items() if c in df.columns]
        if cast_exprs:
            df = df.with_columns(cast_exprs)
    except Exception as schema_error:
        logger.warning(f"Failed to apply predefined schema for {interface_name}: {schema_error}")
问题1.1 — 
validate_data
 的 Polars化

当前第317-324行：data_list = df.to_dicts() + 
_detect_duplicates_fast
。

修改方案：

python
primary_keys = interface_config.get('output', {}).get('primary_key', [])
existing_keys = [k for k in primary_keys if k in df.columns]
if existing_keys:
    dup_count = df.filter(df.is_duplicated()).height
    unique_count = len(df) - dup_count
else:
    dup_count = 0
    unique_count = len(df)
stats = {
    'total': len(df),
    'unique': unique_count,
    'duplicates': dup_count,
    'duplicate_rate': dup_count / len(df) if len(df) > 0 else 0
}
NOTE

修改后 
_detect_duplicates_fast
 函数本身暂时保留（可能外部有调用），但在 
processor.py
 内部不再调用它。

问题1.3 — 
_create_dataframe_row_by_row
 优化

当前：按100行切片、反复 pl.concat(how='diagonal') 开销极大。

修改方案：先尝试 pl.from_dicts(data)（一次性转换），失败时才降级到 chunk concat，并把 chunk 大小从 100 改为 1000，减少 concat 次数：

python
def _create_dataframe_row_by_row(self, data: List[Dict[str, Any]]) -> pl.DataFrame:
    if not data:
        return pl.DataFrame()
    try:
        # 优先：一次性转换（最快路径）
        return pl.from_dicts(data)
    except Exception as e:
        logger.warning(f"pl.from_dicts 失败，降级到分批处理: {e}")
    # 降级：分批concat（chunk 1000 行，减少 concat 次数）
    ...
问题3.4 — 统一日志级别

_handle_primary_keys
 中重复记录从 info 升为 warning（与 
_remove_duplicates
 保持一致）。

问题3.5 — 补充类型注解

为 
_detect_duplicates_fast
、
_estimate_empty_days
（在executor中）等关键函数补充完整返回值类型注解。

Component 3: 
app4/update/update_manager.py
[MODIFY] 
update_manager.py
问题1.2 — 
_update_with_stock_gap_detection
 多线程并发重构

当前：for stock in stock_list: 单线程串行，5000只股票无并发。

修改方案：引入 concurrent.futures.ThreadPoolExecutor，将 _process_single_stock_gaps（新抽取的单只股票处理逻辑）并发提交。并发度从 scheduler 中读取（或使用独立配置项，默认 8）：

python
from concurrent.futures import ThreadPoolExecutor, as_completed
def _update_with_stock_gap_detection(self, ...):
    ...
    max_stock_workers = self.config_loader.global_config.get(
        'update', {}
    ).get('stock_gap_workers', 8)
    
    with ThreadPoolExecutor(max_workers=max_stock_workers) as executor:
        futures = {
            executor.submit(self._process_single_stock_gaps, 
                          interface_name, interface_config, stock, date_range, options): stock
            for stock in stock_list
        }
        for future in as_completed(futures):
            try:
                records = future.result()
                total_records += records
            except Exception as e:
                stock = futures[future]
                logger.error(f"[{interface_name}/{stock.get('ts_code')}] 处理失败: {e}")
IMPORTANT

storage_manager.save_data(async_write=True) 在多线程下是否线程安全需要确认。根据现有代码，save_data 使用了写队列（async_write=True），推测已是线程安全的。如果不确定请在此处注释保守降级策略。

Verification Plan
已有自动化测试
bash
cd /home/quan/testdata/aspipe_v4
python -m pytest app4/tests/test_pagination_executor_atomic_offset.py -v
此测试覆盖 
_execute_single_request
 的原子提交和流式行为，可验证 
pagination_executor.py
 修改后不引入回归。

新增单元测试 (将在执行阶段创建)
在 app4/tests/test_processor_polars.py 中添加：

测试 
_handle_primary_keys
 在有重复行时打印 warning 且不抛出异常
测试 
validate_data
 返回正确的 duplicate_count（通过构造含重复主键的 DataFrame）
测试 
_create_dataframe_row_by_row
 能正确处理正常数据和混合 schema 数据
运行全部测试
bash
cd /home/quan/testdata/aspipe_v4
python -m pytest app4/tests/ -v