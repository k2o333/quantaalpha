开发任务。开始前先读 `/home/quan/testdata/aspipe_v4/docs/00-governance/agent.md`，并按其中要求先读 `docs/00-governance/rules.md`。然后读取任务文档 `/home/quan/testdata/aspipe_v4/docs/03-changes/quantaalpha/planned/parr2/dev/parallel-02-real-backtest-library-ops.task.md`。当前是 dev-only 阶段，测试由后续 test 阶段完成；如果 `agent.md`、`rules.md`、`AGENTS.md` 提到常规验证建议，本次一律不要执行。禁止运行 `pytest`、`python -m pytest`、`coverage run -m pytest` 或任何测试命令；禁止安装任何包；禁止修改允许范围之外的文件。目标是完成 real backtest 内部链路、library summary 和 audit、最小写保护、`continuous_mine.sh` 入口。输出只保留 Modified Files、Behavior Changes、Validation Status、Open Risks、Suggested Test Focus；其中 Validation Status 必须明确写 `Not Run (dev-only stage)`。
